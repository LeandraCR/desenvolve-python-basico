### 3. Pseudocódigo

plaintext
INÍCIO

    // Entrada
    LER n
    
    // Processamento
    dezena <- n - 1
    unidade <- 10 - n
    resultado <- (dezena * 10) + unidade
    
    // Saída
    ESCREVER resultado

FIM
